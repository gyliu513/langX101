# Credentials / cluster secrets — never commit (regenerated by the README steps)
worker1.kubeconfig
hub-ca.crt
*.kubeconfig

# Large upstream installers fetched via curl in the README steps
manifests/kueue-v*.yaml
manifests/jobset-v*.yaml
