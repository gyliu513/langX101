---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: llm-d-sim-prefill
  namespace: llm-d-demo
  labels:
    app: llm-d-sim-prefill
    llm-d.ai/role: prefill
    app.kubernetes.io/part-of: llm-d
spec:
  replicas: 1
  selector:
    matchLabels:
      app: llm-d-sim-prefill
  template:
    metadata:
      labels:
        app: llm-d-sim-prefill
        llm-d.ai/role: prefill
    spec:
      containers:
      - name: vllm-sim
        image: ghcr.io/llm-d/llm-d-inference-sim:v0.8.0-arm64
        imagePullPolicy: IfNotPresent
        args:
        - "--model=Qwen/Qwen2.5-0.5B-Instruct"
        - "--port=8000"
        - "--prefill-time-per-token=80ms"
        - "--prefill-time-std-dev=5ms"
        - "--kv-cache-size=2048"
        - "--block-size=16"
        env:
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        ports:
        - name: http
          containerPort: 8000
          protocol: TCP
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 15
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: llm-d-sim-prefill-svc
  namespace: llm-d-demo
  labels:
    app: llm-d-sim-prefill
    llm-d.ai/role: prefill
spec:
  selector:
    app: llm-d-sim-prefill
  ports:
  - name: http
    port: 8000
    targetPort: http
    protocol: TCP
  type: ClusterIP
